import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => {
  return {
    addButton: {
      position: 'fixed',
      bottom: 32,
      right: 32,
    },
  };
});

export default useStyles;
